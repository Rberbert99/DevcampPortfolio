<h1>Portfolio Index</h1>

<% @portfolio_items.each do |p| %>
  <p><%= link_to p.title, portfolio_path(p) %></p>
  <p><%= p.subtitle %></p>
  <p><%= p.body %></p>
  <p><%= image_tag p.main_image unless p.main_image.nil? %></p>
<% end %>